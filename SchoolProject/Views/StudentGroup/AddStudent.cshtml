@model SchoolProject.ViewModels.StudentGroupIndexData

@{
    ViewBag.Title = "Studen Group";
}

<h2>Add a student</h2>

<div>
    <h4>Add a student to student group</h4>
    <hr />
    <dl class="dl-horizontal">
        
        <dd>
            <table class="table">
                <tr>
                    <th>Sure name</th>
                    <th>First name</th>
                    <th>Email</th>
                    <th>Birthdate</th>
                    <th></th>
                </tr>
                @foreach (var item in Model.Students)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.SureName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.FirstName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Email)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.BirthDate)
                        </td>
                        <td>
                            @Html.ActionLink("Add", "AddStudentByEmail", new { studentEmail = item.Email , id = Model.StudentGroup.StudentGroupID})
                        </td>
                    </tr>
                }
            </table>
        </dd>
    </dl>
</div>
<p>
    @Html.ActionLink("Back to List", "Index", new { id = Model.StudentGroup.StudentGroupID })
</p>
